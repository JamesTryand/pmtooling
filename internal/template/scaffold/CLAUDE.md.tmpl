# Working in this issue

This worktree is scoped to a single `{{.Type}}` issue branch created by `pmt`.

- Read `README.md` first — it carries this issue's metadata (type, title, status) and description.
- Keep changes scoped to this issue; don't touch unrelated branches or worktrees.
- Commit progress on this branch as you go.

Template authors: customize this file per issue type (e.g. a `bug` template might emphasize reproduce -> failing test -> fix -> verify).
